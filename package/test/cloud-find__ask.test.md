# cloud-find ask

## no cloud session

### should fall back to local search instead of failing

```execute
aux4 cloud-find ask "install a package"
```

```error:partial
aux4/cloud-find: no cloud session*?
```

